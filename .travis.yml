# blacklist
#branches:
#  except:
#    - playground-0.5


language: java
jdk:
  - oraclejdk8
  
before_install:
  - "export DISPLAY=:0"
  - "export TERM=dumb"
  - "sh -e /etc/init.d/xvfb start"
  - "cd VRL"

install:
    - TERM=dumb gradle wrapper clean

script:
    - TERM=dumb gradle clean test
