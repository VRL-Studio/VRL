language: java
jdk:
  - oraclejdk7
  - oraclejdk8
  
before_install:
  - "export DISPLAY=:0"
  - "export TERM=dumb"
  - "sh -e /etc/init.d/xvfb :0 -screen 0 800x600x24"
  - "cd VRL"

install:
    - TERM=dumb ant clean

script:
    - TERM=dumb ant test
